import { globalCss } from "@stitches/react";


export const globalStyles = globalCss({
    '*': { 
        margin: 0, 
        padding: 0,
        color: 'White'
    },
    'body': {
        padding: 40,
        background: '$slate1',
    }
});
